/*make function
task it to divide by four
store the value in a variable*/

//my function
const quarterValue = (myNum) => {
    const quarter = myNum / 4
        return quarter
};
const functionResult = quarterValue(80)
console.log(functionResult)